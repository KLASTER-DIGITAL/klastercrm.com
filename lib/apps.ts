/**
 * АДРЕСА ПРИЛОЖЕНИЙ ВИДЖЕТОВ — единственное место хранения.
 *
 * До разделения репозиториев сайт и виджет аналитики жили одним проектом, и
 * кнопка «Открыть демо» вела на относительный `/widget?demo=1`. Теперь сайт —
 * отдельное приложение на klastercrm.com, а виджеты остались каждый в своём:
 * ссылка обязана быть абсолютной, иначе демо отдаёт 404 нашей же страницей.
 *
 * Адрес аналитики совпадает с `APP_ORIGIN` в `widget/script.js` репозитория
 * аналитики — тем самым, который скрипт виджета проверяет у входящих
 * postMessage. Разойдутся эти два значения — сломается не ссылка на сайте, а
 * приём сообщений внутри amoCRM, и увидим мы это не здесь. Меняя одно,
 * меняйте второе.
 *
 * Домен намеренно не klastercrm.com: iframe виджета живёт на своём
 * происхождении, чтобы релиз сайта не мог уронить виджет у клиента.
 */

/** Приложение виджета «Аналитика KLASTER». Репозиторий: AMO Analitics. */
export const ANALYTICS_APP = 'https://klaster-analytics.vercel.app';

/** Приложение виджета «Распределение KLASTER». Репозиторий: KLASTER Distribution. */
export const DISTRIBUTION_APP = 'https://klaster-distribution.vercel.app';

/**
 * Демо-режим аналитики. Режим включается адресом — его читает сам виджет,
 * middleware приложения аналитики без проверенного ключа сессии всё равно
 * увело бы сюда.
 */
export const ANALYTICS_DEMO = `${ANALYTICS_APP}/widget?demo=1`;

/** Вкладка демо. Параметр `tab` переживает загрузку виджета. */
export const analyticsDemoTab = (tab: string): string => `${ANALYTICS_DEMO}&tab=${tab}`;
