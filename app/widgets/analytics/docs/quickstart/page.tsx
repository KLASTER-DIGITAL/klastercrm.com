import type { Metadata } from 'next';
import Link from 'next/link';
import { DocsShell } from '@/app/widgets/analytics/docs/docs-shell';
import { Source, Mark } from '@/app/site/ui';
import { plural, withPlural } from '@/lib/plural';
import { ONBOARDING, PILOT, SAVED_REPORTS_MAX, THRESHOLDS, WIDGET } from '@/lib/company';
import { FILL_RATES, PARKING_INCIDENT } from '@/lib/funnel-data';

/**
 * Быстрый старт. Страница отвечает на один вопрос: что нажать после установки,
 * чтобы получить первую цифру, и в каком порядке.
 *
 * Границы с соседями держатся жёстко, иначе три страницы начинают
 * пересказывать друг друга. Права, письмо администратору и отзыв доступа —
 * на /widgets/analytics/install. Смысл разметки полок — на /docs/stages.
 * Формулы — на /docs/metrics. Пороги заполненности — на /docs/metrics. Здесь
 * только маршрут и грабли на нём.
 *
 * У каждого шага четыре обязательные строки: что делает человек, что делает
 * система, сколько это занимает и что пойдёт не так. Без последней инструкция
 * читается как обещание «всё само», а первый же сбой воспринимается как обман.
 *
 * Тексты шагов 4 и 5 — перенос из lib/messages/help.ts (вкладка «Инструкция»
 * внутри виджета) и из кода: lib/report-state.ts, app/widget/page.tsx
 * (copyLink), app/api/v1/saved-reports/route.ts. Справка, разошедшаяся с
 * поведением продукта, хуже отсутствующей справки.
 */

const ru = new Intl.NumberFormat('ru-RU');

const FIRST_LOAD = withPlural(PILOT.firstLoadMinutes, 'минута', 'минуты', 'минут');
const SYNC_EVERY = withPlural(PILOT.syncEveryMinutes, 'минуту', 'минуты', 'минут');
const INCREMENT = withPlural(PILOT.incrementalSeconds, 'секунда', 'секунды', 'секунд');

/* Вердикт по заполненности считает fillVerdict() в lib/widget-calc.ts. Здесь
   те же пороги применяются к замерам пилота: сколько полей проходит, сколько
   идёт с предупреждением и сколько закрыто. Числа не пишутся руками — иначе
   при следующем замере страница соврёт про собственный продукт. */
const FILL_OK = FILL_RATES.filter((f) => f.rate >= THRESHOLDS.fillWarn).length;
const FILL_WARN = FILL_RATES.filter(
  (f) => f.rate >= THRESHOLDS.fillBlock && f.rate < THRESHOLDS.fillWarn,
).length;
const FILL_BLOCK = FILL_RATES.filter((f) => f.rate < THRESHOLDS.fillBlock).length;
/** Самое заполненное поле пилота — потолок, а не исключение. */
const BEST_FIELD = [...FILL_RATES].sort((a, b) => b.rate - a.rate)[0];

export const metadata: Metadata = {
  title: 'Быстрый старт: от установки до первого отчёта',
  description:
    'Пять шагов: установка, подтверждение разметки этапов, выбор аналитических полей, первый срез, сохранённый отчёт.',
};

interface Step {
  title: string;
  human: React.ReactNode;
  system: React.ReactNode;
  time: React.ReactNode;
  wrong: React.ReactNode;
  /** Строка под чертой: по какому признаку понять, что шаг прошёл правильно. */
  where?: React.ReactNode;
}

const STEPS: readonly Step[] = [
  {
    title: 'Установка и первая загрузка',
    human: (
      <>
        Администратор аккаунта открывает ссылку установки и нажимает «Разрешить». Ссылку выдаём под
        конкретный аккаунт: пока виджета нет в маркетплейсе, публичной кнопки установки не
        существует. Дальше — ждать, ничего нажимать не нужно.{' '}
        <Link href="/widgets/analytics/install">
          Какие права запрашиваются и письмо администратору
        </Link>
        .
      </>
    ),
    system: (
      <>
        Сначала справочники — воронки, этапы, сотрудники, поля сделки. Потом сделки и история смены
        статусов, окнами по датам. Из истории собираются переходы: откуда, куда, когда, кто двигал и
        сколько сделка пролежала на предыдущем этапе. Экран показывает процент, а не крутящийся
        кружок; прерванная загрузка продолжается с той же точки, а не с начала.
      </>
    ),
    time: (
      <>
        Выдача доступа — {withPlural(ONBOARDING.oauthMinutes, 'минута', 'минуты', 'минут')}, это
        оценка. Первая загрузка — {FIRST_LOAD} на {PILOT.historyYears}-летней истории, и это замер:{' '}
        {ru.format(PILOT.leads)} сделок, {ru.format(PILOT.events)} событий,{' '}
        {ru.format(PILOT.transitions)}{' '}
        {plural(PILOT.transitions, 'переход', 'перехода', 'переходов')}. Аккаунт помоложе загрузится
        быстрее: время упирается в объём истории, а не в размер компании. Дальше синхронизация идёт
        инкрементом — {INCREMENT} каждые {SYNC_EVERY}.
      </>
    ),
    wrong: (
      <>
        Интеграция видит ровно то, что видит выдавший доступ. Если у него закрыта часть воронок или
        скрыты уволенные сотрудники, история приедет неполной — и это не будет видно как ошибка,
        просто в отчёте не окажется людей и воронок. Признак: в списке воронок нет тех, что есть в
        CRM, или в «Менеджерах» отсутствует человек, который точно работал в периоде. Лечится
        выдачей доступа от администратора с полной видимостью.
      </>
    ),
    where: (
      <>
        Отчётов по половине истории мы не показываем: до конца первой загрузки на экране процент, а
        не половина воронки. Режим «последние тридцать дней раньше остальных» в плане, но его нет в
        коде — значит, нет и в обещании.
      </>
    ),
  },
  {
    title: 'Подтверждение разметки этапов',
    human: (
      <>
        Посмотреть список этапов, размеченных полками, и сказать, что в нём лишнее. Полка — этап, на
        котором сделка не движется к продаже, а ждёт звонка, решения или сезона. Решение подписывает
        руководитель, а не алгоритм.{' '}
        <Link href="/widgets/analytics/docs/stages">Что такое полка и как её отличить</Link>.
      </>
    ),
    system: (
      <>
        Эвристика проходит по загруженной истории и предлагает разметку. Подтверждённое человеком
        хранится отдельно от вывода эвристики и сильнее его: повторная загрузка и переразметка
        подтверждённые этапы не трогают.
      </>
    ),
    time: (
      <>
        {withPlural(ONBOARDING.stagesMinutes, 'минута', 'минуты', 'минут')} на список — оценка по
        пилоту. Сам разговор о том, что в вашей воронке считать ожиданием, обычно длиннее, и это
        нормально: цифры всего отчёта зависят от этого решения.
      </>
    ),
    wrong: (
      <>
        Эвристика ошибается, и мы это измерили: на полной истории пилота она объявила полками{' '}
        <span className="num">{PARKING_INCIDENT.flagged}</span>{' '}
        {plural(PARKING_INCIDENT.flagged, 'этап', 'этапа', 'этапов')} вместо{' '}
        <span className="num">{PARKING_INCIDENT.real}</span> — включая тот, откуда{' '}
        <span className="num">{PARKING_INCIDENT.contractWinShare}%</span> закрытий уходит в выигрыш.
        Порогом это не чинится, поэтому список и подписывает человек.{' '}
        <Link href="/method/parking">Разбор ошибки целиком, с числами</Link>.
      </>
    ),
    where: (
      <>
        Проверить себя: во вкладке «Воронка» полки вынесены из цепочки и идут отдельным списком.
        Если в этом списке оказался этап, после которого сделки уходят в деньги, разметка неверная —
        и конверсия всей цепочки посчитана не по тем ступеням.
      </>
    ),
  },
  {
    title: 'Выбор аналитических полей',
    human: (
      <>
        Назвать поля сделки, которые нужны в разрезах: проект, источник, причина отказа, тип
        объекта. По умолчанию не синхронизируется ничего сверх системных полей, и поле, способное
        содержать имя, телефон или адрес почты, в список не попадает вовсе.
      </>
    ),
    system: (
      <>
        Заполненность каждого поля считается прямо на выгрузке сделок: счётчик хранит только числа —
        сколько сделок поле заполнили, — а значения полей никуда не записываются. Поэтому
        заполненность известна по всем полям аккаунта, а хранятся единицы из них. Дальше вердикт:{' '}
        <span className="num">{THRESHOLDS.fillWarn}%</span> и выше — разрез строится молча, от{' '}
        <span className="num">{THRESHOLDS.fillBlock}</span> до{' '}
        <span className="num">{THRESHOLDS.fillWarn}%</span> — с предупреждением, ниже{' '}
        <span className="num">{THRESHOLDS.fillBlock}%</span> — не строится.
      </>
    ),
    time: (
      <>
        Отдельного ожидания нет: заполненность считается внутри первой загрузки, тем же проходом по
        сделкам. Отметка поля переживает повторные загрузки — при обновлении справочника она не
        затирается.
      </>
    ),
    wrong: (
      <>
        Полей на сделке много, годных для разрезов — единицы. На пилоте их{' '}
        <span className="num">{PILOT.leadFields}</span>; из{' '}
        {withPlural(FILL_RATES.length, 'поля', 'полей', 'полей')}, которые разбирались как разрезы,
        выше <span className="num">{THRESHOLDS.fillWarn}%</span> —{' '}
        <span className="num">{FILL_OK}</span>, между порогами —{' '}
        <span className="num">{FILL_WARN}</span>, ниже{' '}
        <span className="num">{THRESHOLDS.fillBlock}%</span> —{' '}
        <span className="num">{FILL_BLOCK}</span>. Самое заполненное — «{BEST_FIELD?.field}» на{' '}
        <span className="num">{BEST_FIELD?.rate}%</span>. Это не поломка виджета и не наша
        придирчивость: разрез по такому полю показал бы, кто что заполняет, а не как идут продажи.{' '}
        <Link href="/widgets/analytics/docs/metrics">Что делать с полем ниже порога</Link>.
      </>
    ),
    where: (
      <>
        Вкладка «Качество данных» пишет вердикт словами: «можно считать», «разрез покроет меньше
        половины», «разрез строить нельзя». Отдельно там же «не считалась» — это не ноль, а
        отсутствие замера.
      </>
    ),
  },
  {
    title: 'Первый срез',
    human: (
      <>
        Выбрать воронку и период — этого уже достаточно, чтобы увидеть цифры. Дальше сужается:
        группа, менеджер, поле сделки. «Сравнение» добавляет второй период, и виджет считает дельты.
      </>
    ),
    system: (
      <>
        Строка фильтров действует на все вкладки сразу, переключение вкладки ничего не сбрасывает:
        обзор, воронка, путь заявки, путь клиента, менеджеры, качество данных и AI-разбор считаются
        из одного среза. Когда период — целый календарный месяц, на «Обзоре» появляется карточка
        плана: сделки, успешные, выручка, выполнение и прогноз по темпу. Факт в ней берётся по всей
        воронке, фильтры его не сужают.
      </>
    ),
    time: (
      <>
        Замера нет. Срез считается по уже загруженным данным — в amoCRM виджет в этот момент не
        ходит, и смена фильтра не запускает новую синхронизацию.
      </>
    ),
    wrong: (
      <>
        Виджет местами откажется показать процент. При базе меньше{' '}
        <span className="num">{THRESHOLDS.minBase}</span> сделок вместо него стоит «мало данных», и
        это не ноль. Конверсия выше{' '}
        <span className="num">{THRESHOLDS.conversionAnomaly}%</span> — тоже не ошибка: в этап
        приходят не только из предыдущего, и такое значение помечается, а не прячется. Переходы,
        сделанные автоматикой, идут отдельной строкой и не входят в медиану отдела.{' '}
        <Link href="/widgets/analytics/docs/metrics">Откуда берётся каждое число</Link>.
      </>
    ),
    where: (
      <>
        Начинать стоит не с «Воронки», а с «Качества данных»: от него зависит, какие разрезы вообще
        имеет смысл открывать. Вторым — «Воронка»: там сразу видно, верна ли разметка из шага 2.
      </>
    ),
  },
  {
    title: 'Сохранённый отчёт',
    human: (
      <>
        Кнопка «Отчёты» → «Сохранить текущий…», название и, если отчёт нужен коллегам, галочка
        «Виден всей команде». Ссылка на срез — кнопка «Скопировать ссылку на отчёт» в строке
        фильтров.
      </>
    ),
    system: (
      <>
        Отчёт запоминает срез, открытую вкладку, набор блоков печати и заголовок. Менеджер и проект
        хранятся устойчивыми ключами — идентификатором пользователя amoCRM и значением поля, — а не
        номерами в списке: списки пересобираются на каждый период, и отчёт, сохранённый номером,
        завтра показал бы чужие цифры под тем же названием. Ссылка у сохранённого отчёта короткая —
        идентификатор отчёта и язык, без перечисления фильтров: получатель открывает свежую версию
        отчёта с сервера, а не замороженный набор параметров, каким он был в момент копирования.
        Ключ сессии в ссылку не попадает никогда — отдать ссылку не
        значит отдать доступ, получатель проходит своё удостоверение. Внутри amoCRM ссылка ведёт на
        страницу виджета в самой CRM.
      </>
    ),
    time: (
      <>
        Одно нажатие. Отчётов на автора не больше{' '}
        <span className="num">{SAVED_REPORTS_MAX}</span>: на попытке сохранить следующий виджет
        скажет «Лимит {withPlural(SAVED_REPORTS_MAX, 'отчёт', 'отчёта', 'отчётов')} на пользователя.
        Удалите ненужные», а не потеряет отчёт молча.
      </>
    ),
    wrong: (
      <>
        Менеджер уволился, проект исчез из данных, воронку удалили — фильтр честно сбрасывается на
        «все», и сверху появляется строка «Менеджера из отчёта нет в этом периоде — показаны все
        менеджеры». Соседа по списку виджет вместо него не подставляет. Удалённый отчёт по ссылке
        отвечает «Отчёт не открылся. Возможно, его удалили». В демо сохранённых отчётов нет вовсе:
        они работают при входе из amoCRM, и сохранять срез по выдуманным данным было бы выдумкой.
      </>
    ),
    where: (
      <>
        Личный и общий различаются правами, а не видом. Общий открывают все в аккаунте;
        переименовать, перезаписать, снять «общий» и удалить может только автор — чужой отчёт на
        правку отвечает «не найдено» и не подсказывает, что он вообще существует.
      </>
    ),
  },
];

/** Первое, что стоит открыть, когда загрузка закончилась. Порядок — не алфавит. */
const FIRST_LOOK: readonly { tab: string; why: React.ReactNode; href: string }[] = [
  {
    tab: 'Качество данных',
    why: 'Заполненность каждого поля и вердикт по нему. Определяет, какие разрезы имеет смысл открывать, а какие виджет строить откажется.',
    href: '/widgets/analytics/docs/metrics',
  },
  {
    tab: 'Воронка',
    why: 'Конверсия между соседними ступенями и полки отдельным списком. Проверка разметки из шага 2: чужой этап в списке полок видно сразу.',
    href: '/widgets/analytics/docs/stages',
  },
  {
    tab: 'Путь заявки',
    why: 'Откаты назад, пропуски ступеней и уходы в другие воронки. Здесь обычно и находится ответ на «куда делись заявки».',
    href: '/widgets/analytics/docs/metrics',
  },
];

export default function QuickstartPage() {
  return (
    <DocsShell
      active="quickstart"
      title="Быстрый старт: от установки до первого отчёта"
      lead="Что нажать после установки, что в это время делает система и на чём обычно спотыкаются — от выданного доступа до отчёта, который открывает вся команда."
    >
      <div className="site-status">
        <span>пять шагов</span>
        <span>первая загрузка {FIRST_LOAD} — замер</span>
        <span>дальше {INCREMENT} каждые {SYNC_EVERY}</span>
        <span>версия {WIDGET.version}</span>
      </div>
      <Source>
        замер {PILOT.measuredAt} · первая полная загрузка пилотного аккаунта ({PILOT.who}) ·{' '}
        {PILOT.source}
      </Source>

      <p className="site-p" style={{ marginTop: 24 }}>
        Права, письмо администратору и что происходит после отзыва доступа разобраны на странице{' '}
        <Link href="/widgets/analytics/install">подключения</Link>. Здесь — путь до первой цифры.
        Двух шагов из пяти в интерфейсе пока нет: разметку этапов и список аналитических полей мы
        включаем на своей стороне, по вашему решению. Об этом ниже, отдельным блоком, а не мелким
        шрифтом.
      </p>

      <h2 className="site-h2">Пять шагов по порядку</h2>
      <div className="site-rules">
        {STEPS.map((step, i) => (
          <section key={step.title} className="site-rule">
            <div className="site-rule__n">{i + 1}</div>
            <div className="site-rule__body">
              <h3 className="site-h3">{step.title}</h3>
              <p className="site-p">
                <b>Вы.</b> {step.human}
              </p>
              <p className="site-p">
                <b>Система.</b> {step.system}
              </p>
              <p className="site-p">
                <b>Сколько.</b> {step.time}
              </p>
              <p className="site-p">
                <b>Что пойдёт не так.</b> {step.wrong}
              </p>
              {step.where ? (
                <p className="site-rule__where">
                  <b>Как проверить.</b> {step.where}
                </p>
              ) : null}
            </div>
          </section>
        ))}
      </div>
      <Source kind="estimate">
        выдача доступа — {withPlural(ONBOARDING.oauthMinutes, 'минута', 'минуты', 'минут')},
        подтверждение разметки —{' '}
        {withPlural(ONBOARDING.stagesMinutes, 'минута', 'минуты', 'минут')}: секундомером мерена
        только первая загрузка, остальное — оценка по пилоту
      </Source>

      <h2 className="site-h2">Два шага, у которых пока нет экрана</h2>
      <p className="site-p">
        Оба решения работают и хранятся в базе — не написан интерфейс, которым их принимают
        самостоятельно. Пока это делается вместе с нами при подключении и по письму в поддержку:
        аккаунт один, разговор всё равно происходит.
      </p>
      <div className="site-grid site-grid--2">
        <section className="site-card">
          <div className="site-cardhead">
            <h3 className="site-h3">Подтверждение разметки этапов</h3>
            <Mark kind="building">экран готовится</Mark>
          </div>
          <p className="site-p">
            Эвристика размечает сама, подтверждённое человеком её перебивает и переживает повторные
            загрузки. Кнопки «это не полка» в виджете пока нет: пришлите список — поправим и
            пересчитаем. Список полок при этом виден всегда, отдельным блоком во вкладке «Воронка»,
            так что проверить разметку можно без нас.
          </p>
        </section>
        <section className="site-card">
          <div className="site-cardhead">
            <h3 className="site-h3">Список аналитических полей</h3>
            <Mark kind="building">экран готовится</Mark>
          </div>
          <p className="site-p">
            Заполненность считается по всем полям аккаунта и видна во вкладке «Качество данных».
            Отметка «это поле нужно в разрезах» ставится на нашей стороне и при обновлении
            справочника не затирается. Поля, способные содержать персональные данные, в список не
            попадают ни по чьей просьбе.
          </p>
        </section>
      </div>
      <p className="site-p" style={{ marginTop: 16 }}>
        Полный список того, чего продукт ещё не умеет, лежит по отдельному адресу —{' '}
        <Link href="/not-ready">чего мы ещё не умеем</Link>.
      </p>

      <h2 className="site-h2">Что открыть первым, когда загрузка закончилась</h2>
      <p className="site-p">
        Порядок не случайный. Начинать с «Воронки» соблазнительно, но если поля пусты, а полки
        размечены неверно, красивая картинка окажется первой цифрой, которую придётся отзывать.
      </p>
      <div className="site-grid site-grid--3">
        {FIRST_LOOK.map((item, i) => (
          <section key={item.tab} className="site-card">
            <h3 className="site-h3">
              {i + 1}. {item.tab}
            </h3>
            <p className="site-p">{item.why}</p>
            <p className="site-p">
              <Link href={item.href}>Как это считается</Link>
            </p>
          </section>
        ))}
      </div>

      <h2 className="site-h2">Пройти маршрут без установки</h2>
      <p className="site-p">
        Шаги 4 и 5 можно потрогать прямо сейчас: в{' '}
        <Link href="/widgets/analytics/demo">демо</Link> работают тот же фильтр и те же вкладки на
        обезличенных данных. Сохранённых отчётов там нет — они привязаны к аккаунту amoCRM, и
        сохранять срез по демо-данным было бы выдумкой.
      </p>
      <p className="site-p" style={{ marginTop: 20 }}>
        <Link className="btn" href="/widgets/analytics/demo">
          Открыть демо
        </Link>{' '}
        <Link className="btn btn--ghost" href="/widgets/analytics/install">
          Как подключить
        </Link>
      </p>
    </DocsShell>
  );
}
