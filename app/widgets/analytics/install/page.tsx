import type { Metadata } from "next";
import Link from "next/link";
import { plural, withPlural } from "@/lib/plural";
import { SiteShell } from "@/app/site/shell";
import { Source, Mark } from "@/app/site/ui";
import {
  AMO_SCOPES,
  COMPANY,
  ONBOARDING,
  PILOT,
  THRESHOLDS,
  WIDGET,
} from "@/lib/company";
import { FILL_RATES } from "@/lib/funnel-data";

/**
 * Онбординг вне кабинета. Страницу открывают в двух ролях, и обе надо обслужить
 * на одном экране: администратор — чтобы понять, что именно он подписывает, и
 * руководитель отдела продаж, который администратором не является и которому
 * нужно готовое письмо, а не пересказ своими словами.
 *
 * Письмо лежит текстом внутри страницы, а не за кнопкой «скопировать»: кнопка
 * потянула бы 'use client' ради одного обработчика, а выделить абзац мышью
 * человек умеет и без нас.
 *
 * Числа руками не пишутся: сроки — из PILOT и ONBOARDING, права — из
 * AMO_SCOPES, пороги — из THRESHOLDS. Письмо собирается из тех же констант,
 * поэтому оно не может разойтись с текстом страницы над ним.
 */

const ru = new Intl.NumberFormat("ru-RU");

/** Из пяти прав amoCRM мы просим одно — считаем, а не пишем цифру руками. */
const ASKED = AMO_SCOPES.filter((s) => s.asked);

const FIRST_LOAD = withPlural(
  PILOT.firstLoadMinutes,
  "минута",
  "минуты",
  "минут",
);
const SYNC_EVERY = withPlural(
  PILOT.syncEveryMinutes,
  "минуту",
  "минуты",
  "минут",
);

/** Пример поля, по которому разрез не строится: число берём из замеров. */
const WEAK_FIELD = FILL_RATES.find((f) => f.field === "Источник");

export const metadata: Metadata = {
  title: "Как подключить виджет аналитики к amoCRM",
  description:
    "Кто может выдать доступ, какие права запрашиваются, сколько идёт первая загрузка " +
    `(замер: ${FIRST_LOAD} на ${PILOT.historyYears}-летней истории) и что делать при отзыве доступа.`,
};

/**
 * Шаги подключения. У каждого две строки: что делает система и что делает
 * человек. Без второй строки инструкция читается как обещание «всё само», а
 * дальше выясняется, что разметку этапов кто-то должен подписать.
 */
const STEPS: {
  title: string;
  system: React.ReactNode;
  human: React.ReactNode;
}[] = [
  {
    title: "Выдача доступа по OAuth",
    system: (
      <>
        Принимаем пару токенов, записываем аккаунт и дальше обновляем доступ
        сами. Пароль администратора при этом нигде не вводится и к нам не
        попадает: amoCRM спрашивает его у себя.
      </>
    ),
    human: (
      <>
        Администратор открывает ссылку установки и нажимает «Разрешить». Ссылку
        выдаём в ответ на запрос: пока виджета нет в маркетплейсе, публичной
        кнопки установки не существует.
      </>
    ),
  },
  {
    title: "Первичная загрузка истории",
    system: (
      <>
        Забираем справочники — воронки, этапы, пользователей, поля, — затем
        сделки и историю смены статусов окнами по датам. Из истории собираются
        переходы: откуда, куда, когда, кто двигал, сколько сделка пролежала на
        предыдущем этапе.
      </>
    ),
    human: (
      <>
        Ждать. Экран показывает процент, а не крутящийся кружок. Если процесс
        прервётся, он продолжится с той же точки, а не с начала.
      </>
    ),
  },
  {
    title: "Подтверждение разметки этапов",
    system: (
      <>
        Эвристика проходит по истории и предлагает, какие этапы считать полками:
        сделка на них не движется к продаже, а ждёт звонка, решения или сезона.
      </>
    ),
    human: (
      <>
        Руководитель отдела продаж подтверждает список или правит его.
        Подписывает человек, а не алгоритм: на полной истории пилота эвристика
        ошиблась, и порогом мы это чинить не стали —{" "}
        <Link href="/method/parking">разбор ошибки целиком</Link>. Кнопки «это не
        полка» в виджете пока нет: список присылаете письмом, мы проставляем и
        пересчитываем. Сам список полок при этом виден всегда — отдельным блоком
        во вкладке «Воронка».
      </>
    ),
  },
  {
    title: "Выбор аналитических полей",
    system: (
      <>
        Считаем заполненность каждого поля сделки и показываем её числом. Поле,
        заполненное меньше чем у {THRESHOLDS.fillBlock}% сделок, в разрез не
        пойдёт; между {THRESHOLDS.fillBlock} и {THRESHOLDS.fillWarn}% — пойдёт с
        предупреждением в шапке отчёта.
      </>
    ),
    human: (
      <>
        Администратор называет поля, которые нужны в разрезах. По умолчанию не
        синхронизируется ничего сверх системных: поле, способное содержать имя,
        телефон или почту, в белый список не попадает. Экрана для этого шага тоже
        пока нет — отметку ставим мы, по вашему письму, и при обновлении
        справочника она не затирается.
      </>
    ),
  },
  {
    title: "Первый отчёт",
    system: (
      <>
        Все вкладки считаются из одного среза: воронка, период, группа,
        менеджер, поле сделки. Переключение вкладки ничего не сбрасывает.
      </>
    ),
    human: (
      <>
        Выбрать воронку и период. Дальше вопросы обычно к цифрам, а не к
        интерфейсу, —{" "}
        <Link href="/widgets/analytics/docs/metrics">
          формулы разобраны отдельно
        </Link>
        .
      </>
    ),
  },
];

/**
 * Письмо администратору. Собирается из тех же констант, что и страница: если
 * замер поменяется, письмо поменяется вместе с ним, а не останется жить с
 * прошлогодним числом в чужой почте.
 */
const LETTER = `Тема: доступ для виджета аналитики KLASTER в нашем amoCRM

Прошу подключить к нашему аккаунту amoCRM виджет аналитики воронки KLASTER. Он считает конверсию между соседними этапами и отдельно показывает этапы, на которых сделка ждёт, а не движется к продаже. Установить его может только администратор: доступ в amoCRM выдаёт конкретный человек.

Что запрашивается: ${withPlural(ASKED.length, "право", "права", "прав")} из ${AMO_SCOPES.length} — «${ASKED.map((s) => s.name).join("», «")}». Доступ к файлам, их удаление, центр уведомлений и AI-сервис amoCRM не запрашиваются.

Виджет только читает. Отдельного права «только чтение» в amoCRM нет, поэтому проверять надо не галочку в окне доступа, а поведение: в коде виджета нет ни одного метода записи в amoCRM. Имена, телефоны, адреса почты и тексты переписок в базу виджета не попадают вовсе.

Первая загрузка истории — ${FIRST_LOAD} на ${PILOT.historyYears}-летней истории. Это замер на пилотном аккаунте, а не расчёт. Дальше синхронизация занимает ${PILOT.incrementalSeconds} ${plural(PILOT.incrementalSeconds, "секунду", "секунды", "секунд")} каждые ${SYNC_EVERY}.

Доступ отзывается без нашего участия и в любой момент: карточка интеграции, вкладка «Выданные доступы».

Список прав целиком и что происходит после отзыва: ${COMPANY.url}/widgets/analytics/install`;

export default function InstallPage() {
  return (
    <SiteShell active="/widgets/analytics">
      <h1 className="site-h1">Как подключить виджет аналитики к amoCRM</h1>
      <p className="site-lead">
        Доступ выдаёт администратор аккаунта одной кнопкой в amoCRM и там же
        отзывает. Дальше работает загрузка истории: {FIRST_LOAD} на{" "}
        {PILOT.historyYears}-летней истории пилота — это замер, а не расчёт.
      </p>
      <div className="site-status">
        <Mark kind="building">в маркетплейсе — на модерации</Mark>
        <span>ставится приватной ссылкой</span>
        <span>ставит администратор</span>
        <span>только чтение</span>
        <span>персональные данные не хранятся</span>
      </div>
      <Source>
        версия {WIDGET.version} · технический аккаунт amoCRM с{" "}
        {WIDGET.techAccountSince} · публичная сборка принята валидатором, сроков
        модерации amoCRM не публикует
      </Source>

      <h2 className="site-h2">Пункта в разделе «Аналитика» пока не будет</h2>
      <p className="site-p">
        До публикации в маркетплейсе виджет ставится приватной интеграцией, а
        она не принимает пункт меню: манифест с ним отвергается при загрузке
        архива, без него тот же архив принимается. Это ограничение amoCRM,
        проверенное перебором, а не недоделка установки. Виджет открывается
        своей страницей из списка виджетов аккаунта.
      </p>
      <p className="site-p">
        Код к публикации готов: обработчик пункта меню уже написан и включится
        без единой правки, как только модерация пройдена.
      </p>

      <h2 className="site-h2">Ставит только администратор</h2>
      <p className="site-p">
        Доступ к API amoCRM выдаёт конкретный человек, и он виден в аккаунте —
        карточка интеграции, вкладка «Выданные доступы». Интеграция видит ровно
        то, что видит выдавший доступ: если у него закрыта часть воронок или
        скрыты уволенные сотрудники, история переходов приедет неполной. Поэтому
        доступ лучше выдавать администратору с полной видимостью и такому,
        который останется в компании.
      </p>

      <h2 className="site-h2">
        Если вы не администратор — письмо, которое можно отправить
      </h2>
      <p className="site-p">
        Текст ниже отвечает на все вопросы, которые администратор задаст в
        ответ: что за виджет, какие права, кто гарантирует чтение, сколько это
        займёт и как отключить. Выделите и скопируйте — кнопки «скопировать»
        здесь нет намеренно.
      </p>
      <div className="site-card" style={{ marginTop: 16 }}>
        <pre
          style={{
            whiteSpace: "pre-wrap",
            fontFamily: "inherit",
            fontSize: 14.5,
            lineHeight: 1.65,
            color: "var(--ink-soft)",
            margin: 0,
          }}
        >
          {LETTER}
        </pre>
      </div>
      <p className="site-p" style={{ marginTop: 16 }}>
        Ссылку установки в письме нет: она выдаётся под конкретный аккаунт.{" "}
        <Link href="/support">Запросите её у нас</Link> — придёт в тот же день.
      </p>

      <h2 className="site-h2">Пять шагов по порядку</h2>
      <div className="site-rules">
        {STEPS.map((step, i) => (
          <section key={step.title} className="site-rule">
            <div className="site-rule__n">{i + 1}</div>
            <div className="site-rule__body">
              <h3 className="site-h3">{step.title}</h3>
              <p className="site-p">
                <b>Система.</b> {step.system}
              </p>
              <p className="site-p">
                <b>Вы.</b> {step.human}
              </p>
            </div>
          </section>
        ))}
      </div>
      <Source kind="estimate">
        выдача доступа —{" "}
        {withPlural(ONBOARDING.oauthMinutes, "минута", "минуты", "минут")},
        подтверждение разметки —{" "}
        {withPlural(ONBOARDING.stagesMinutes, "минута", "минуты", "минут")}:
        оценка по пилоту, секундомером мерена только первая загрузка
      </Source>

      <h2 className="site-h2">Какие права запрашиваются</h2>
      <p className="site-p">
        В окне выдачи доступа amoCRM показывает пять прав. Мы просим одно. Ниже
        — все пять и что мы делаем с каждым, включая четыре, которые не берём.
      </p>
      <table className="site-table">
        <thead>
          <tr>
            <th>Право</th>
            <th>Запрашиваем</th>
            <th>Почему</th>
          </tr>
        </thead>
        <tbody>
          {AMO_SCOPES.map((scope) => (
            <tr key={scope.name}>
              <td data-label="Право">{scope.name}</td>
              <td data-label="Запрашиваем">{scope.asked ? "да" : "нет"}</td>
              <td data-label="Почему">{scope.why}</td>
            </tr>
          ))}
        </tbody>
      </table>
      <p className="site-p" style={{ marginTop: 20 }}>
        Отдельного права «только чтение» в amoCRM не существует: «Данные
        аккаунта» покрывает все методы API, включая запись. Мы это не прячем и
        галочкой read-only не прикрываемся. Чтение держится с нашей стороны: в
        клиенте amoCRM нет ни одного метода записи, а прямой запрос мимо клиента
        не проходит проверку сборки.
      </p>
      <p className="site-p">
        Что именно читается и чего в базе нет ни в одной таблице — на странице{" "}
        <Link href="/security">Данные и доступ</Link>.
      </p>
      <Source>
        список прав сверен по документации разработчика amoCRM{" "}
        {WIDGET.rightsCheckedAt} · пять прав: данные аккаунта, файлы, удаление
        файлов, центр уведомлений, AI-сервис
      </Source>

      <h2 className="site-h2">Сколько идёт первая загрузка</h2>
      <div className="site-grid site-grid--2">
        <section className="site-card">
          <h3 className="site-h3">
            {FIRST_LOAD} на {PILOT.historyYears}-летней истории
          </h3>
          <p className="site-p">
            Замер на пилотном аккаунте: {ru.format(PILOT.leads)} сделок,{" "}
            {ru.format(PILOT.events)} событий, {ru.format(PILOT.transitions)}{" "}
            {plural(PILOT.transitions, "переход", "перехода", "переходов")}.
            Аккаунт помоложе загрузится быстрее — время упирается в объём
            истории, а не в размер компании.
          </p>
        </section>
        <section className="site-card">
          <h3 className="site-h3">
            Дальше — {PILOT.incrementalSeconds} секунд каждые {SYNC_EVERY}
          </h3>
          <p className="site-p">
            Инкремент догружает только изменённое, полный проход в{" "}
            {withPlural(PILOT.fullPassSeconds, "секунду", "секунды", "секунд")}{" "}
            больше не нужен. Свежесть данных держит расписание: приёмник
            вебхуков ещё не написан, и обещать мгновенную реакцию на движение
            сделки мы не будем.
          </p>
        </section>
      </div>
      <p className="site-p" style={{ marginTop: 16 }}>
        Раньше мы писали «4–6 минут». Это была арифметика при выбранном пределе запросов, а не
        замер: настоящая скорость ответа amoCRM на истории событий оказалась кратно ниже, и оценка
        ошиблась в тридцать раз. Ускорить загрузку можно параллелизмом окон — он в плане; пока на
        сайте стоит измеренное.
      </p>
      <p className="site-p" style={{ marginTop: 16 }}>
        Смотреть отчёты по частично загруженной истории пока нельзя: до конца
        первой загрузки экран показывает процент, а не половину воронки. Режим
        «последние тридцать дней раньше остальных» в плане, но его нет в коде,
        поэтому его нет и в этом обещании.
      </p>
      <p className="site-p">
        Загрузку стоит согласовать по времени. Лимит запросов к API делится
        между всеми интеграциями аккаунта — телефонией, чатами, другими
        виджетами, — и превышение бьёт по всему аккаунту, а не по одному
        виджету. Мы держим свою долю с запасом и умеем ждать, но знать про
        соседей по аккаунту полезно заранее.
      </p>
      <Source>
        замер {PILOT.measuredAt} · первая полная загрузка пилотного аккаунта (
        {PILOT.who}) · {PILOT.source}
      </Source>

      <h2 className="site-h2">Если доступ отозвали</h2>
      <p className="site-p">
        Отзыв — обычное действие администратора, а не авария: карточка
        интеграции, вкладка «Выданные доступы», кнопка. Нашего согласия для
        этого не нужно, и предупреждать нас заранее незачем.
      </p>
      <div className="site-grid site-grid--3">
        <section className="site-card">
          <h3 className="site-h3">Виджет это видит</h3>
          <p className="site-p">
            Первый же запрос к amoCRM возвращает отказ. Виджет показывает
            «доступ отозван» и просит переавторизацию, а не рисует нули вместо
            чисел.
          </p>
        </section>
        <section className="site-card">
          <h3 className="site-h3">Синхронизация останавливается</h3>
          <p className="site-p">
            Новых данных не поступает — читать нечем. Всё, что было загружено до
            отзыва, остаётся в базе и продолжает считаться.
          </p>
        </section>
        <section className="site-card">
          <h3 className="site-h3">Возврат — это тот же шаг 1</h3>
          <p className="site-p">
            Администратор выдаёт доступ заново, синхронизация продолжается с той
            точки, где остановилась. Историю заново не выкачиваем.
          </p>
        </section>
      </div>
      <p className="site-p" style={{ marginTop: 16 }}>
        Отдельно:{" "}
        <b>
          секретный ключ интеграции нельзя перевыпускать при работающей
          синхронизации
        </b>
        . amoCRM предупреждает об этом прямым текстом, и предупреждение
        буквально — перевыпуск удаляет все выданные доступы разом, вкладка
        «Выданные доступы» становится пустой, а загрузка падает на середине. У
        нас так уже было: загрузка шла несколько часов и оборвалась именно по
        этой причине.
      </p>

      <h2 className="site-h2">Что дальше</h2>
      <p className="site-p">
        Виджет можно посмотреть до всякого доступа: демо открыто без регистрации
        и работает на обезличенных данных пилотного аккаунта. Там же видно, как
        выглядит вкладка «Качество данных», ради которой половина этой
        инструкции про заполненность полей — на пилоте разрез по источнику не
        строится, потому что поле заполнено у{" "}
        {WEAK_FIELD ? `${WEAK_FIELD.rate}%` : "малой доли"} сделок.
      </p>
      <div className="site-actions">
        <Link className="btn" href="/widgets/analytics/demo">
          Открыть демо
        </Link>
        <Link
          className="btn btn--ghost"
          href="/widgets/analytics/docs/quickstart"
        >
          Быстрый старт
        </Link>
        <Link className="btn btn--ghost" href="/widgets/analytics/docs">
          Вся документация
        </Link>
        <Link className="btn btn--ghost" href="/support">
          Запросить ссылку установки
        </Link>
      </div>
    </SiteShell>
  );
}
