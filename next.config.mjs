/** @type {import('next').NextConfig} */
const nextConfig = {
  // Виджет этапа 4 будет жить в iframe внутри amoCRM — заголовки безопасности
  // настраиваются тогда же. Лендинг и кабинет — обычные страницы.
  poweredByHeader: false,
  images: {
    /**
     * К складскому набору добавлена одна ступень — 2240.
     *
     * Кадры продукта сняты при 1360 CSS-пикселях и показываются в колонке
     * шириной не больше 1112: на ретине рабочего стола нужно ровно 2224
     * пикселя. Ближайшая складская ступень выше — 3840, и браузер брал её,
     * то есть тянул исходник целиком (2720 px) там, где хватает 2240.
     * На странице с тремя кадрами это 437 КБ против 315 КБ.
     *
     * 3840 оставлена: на рабочем столе с плотностью 3 ниже неё ступени нет.
     */
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 2240, 3840],
  },
};
export default nextConfig;
